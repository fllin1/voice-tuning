# Optional: Hume Octave (cloud TTS). Leave blank to disable Hume in the UI.
HUME_API_KEY=

# Optional: override the local SQLite database path. Default: ./voice-tuning.db
# VT_DB_PATH=./voice-tuning.db

# Optional: override the audio cache directory. Default: ./audio_cache
# VT_AUDIO_CACHE_DIR=./audio_cache
